eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://registry-service:8761/eureka/
    healthcheck:
       enabled: true

spring:
  data:
    mongodb:
      host: team-db
      port: 27081
      database: team_db